## 1. Problem Breakdown
Restate the problem in plain language. Clarify input format, output format, and constraints (size limits, value ranges, whether duplicates/negatives are allowed, etc.).

## 2. Test Cases
- The given example(s), walked through step by step.
- Edge cases: empty input, single element, all duplicates, all same value, negative numbers, already sorted/reverse sorted, max constraint size, overflow-prone values, etc. (pick the ones relevant to this problem).

## 3. Pattern Recognition
Identify which DSA pattern(s) the problem fits — Two Pointers, Sliding Window, Fast & Slow Pointers, Binary Search, DFS/BFS, Backtracking, Dynamic Programming, Greedy, Union-Find, Heap/Priority Queue, Prefix Sum, Monotonic Stack/Queue, Trie, Topological Sort, Bit Manipulation, etc. Explain *why* the shape of the problem signals that pattern (e.g. "sorted array + pair sum → two pointers", "contiguous subarray/substring with a constraint → sliding window").

## 4. Brute Force Solution
- One-paragraph reasoning.
- Working, compilable C# code.
- Time and space complexity, with a one-line justification.

## 5. Optimal Solution
- Explain the key insight that eliminates the brute force's redundant work.
- Working, compilable C# code, following the pattern identified in section 3.
- Time and space complexity, with a one-line justification.

## 6. Complexity Comparison
A short table: Approach | Time | Space, plus one sentence on the tradeoff (e.g. optimal trades space for time, or is strictly better).

